machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install pandoc
    - pip install -U pip
    - pip install xml2rfc

test:
  override:
    - make draft-rosenbergjennings-dispatch-ripp-04.txt
    - head draft-rosenbergjennings-dispatch-ripp-04.txt

general:
  artifacts:
    - draft-rosenbergjennings-dispatch-ripp-04.txt
    - draft-rosenbergjennings-dispatch-ripp-04.html